command:
  - streamlit
  - run
  - app.py
  - --server.port
  - "8000"
  - --server.address
  - 0.0.0.0
env:
  # Tilpas catalog.schema.table til jeres Unity Catalog
  - name: RESTORE_REQUESTS_TABLE
    value: main.restore.restore_requests
  - name: RESTORE_ADMIN_GROUP
    value: admins
  # Sæt warehouse via valueFrom hvis I har sql_warehouse resource, fx:
  # - name: DATABRICKS_WAREHOUSE_ID
  #   valueFrom: sql_warehouse
